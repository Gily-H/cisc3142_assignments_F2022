name: Hello World
on: [push]
jobs:
        Run_All_Labs:
                runs-on: ubuntu-latest
                steps:
                        - uses: actions/checkout@v3
                        - run: echo "Running makefile"
                        - name: Lab Exercises
                          run: make ci_cd_workflow
