#include <iostream>
#include <string>
#include <vector>

using std::string;
using std::vector;
using std::cin;
using std::cout;
using std::endl;


/*
 * assumes user inputs words one at a time
 */
void to_uppercase_words() {
  // initialize an empty string vector
  vector<string> sequence;
  string word;

  // while (there is input to read) 
    // push_back word input to end of vector

  // for (string in vector)
    // for (reference char in string)
      // uppercase char
    
  // for (string in vector) 
    // if at the position divisible by 9
      // add a new line
    // print word without new line
}

int main() {
  to_uppercase_words();
  return 0;
}